<?php

// Hier trage bitte die Daten (id's) von den zuvor erstellten Custom Columns ein.
// Please enter the data (id's) from the custom columns you created.
$host_ip  = get_api_db($user_id,0);
$key      = get_api_db($user_id,1);
$key_pass = get_api_db($user_id,2);